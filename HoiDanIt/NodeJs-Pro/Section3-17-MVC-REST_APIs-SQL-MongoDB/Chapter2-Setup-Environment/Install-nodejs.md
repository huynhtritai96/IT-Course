If your current Node.js came from `apt`, remove it first:
```bash
sudo apt remove -y nodejs
sudo apt autoremove -y
```

# Verify
```
node -v
npm -v
```

Run it with:
```bash
bash install-node-22.13.0.sh
```

